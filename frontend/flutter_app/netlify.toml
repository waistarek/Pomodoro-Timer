[build]
  base = "frontend/flutter_app"
  command = "flutter build web --release --dart-define=API_BASE_URL=$API_BASE_URL"
  publish = "build/web"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
